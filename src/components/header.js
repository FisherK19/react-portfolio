import React from 'react';
import NavBar from '../components/navbar';

const Header = () => {
    return (
        <React.Fragment>
            <NavBar />
        </React.Fragment>
    );
};

export default Header;
